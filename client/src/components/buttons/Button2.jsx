import React from 'react'
import "./button2.scss";
const Button2 = (props) => {
    return (
        <button className='rotating-background-button'>{props.name}</button>
    )
}

export default Button2